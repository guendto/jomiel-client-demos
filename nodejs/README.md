# Nodejs example

## Building

With npm:

```shell
npm i               # Install deps
npm run gen:static  # Compile protobuf declarations for jomiel messages
```

With [pnpm]:

```shell
pnpm i
pnpm gen:static
```

[pnpm]: https://pnpm.io/

## Running

```shell
node . [args ...]
```
